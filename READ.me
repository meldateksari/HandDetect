# (Opsiyonel) proje klasörü
mkdir el-ile-kontrol && cd el-ile-kontrol

# Sanal ortam
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS / Linux
source .venv/bin/activate
source /Users/meldateksari/Desktop/Projects/HandDetect/.venv/bin/activate
# Paketler
pip install opencv-python mediapipe==0.10.14 pyautogui numpy

------------------------
Nasıl çalıştırılı?

python -m venv .venv
.venv\Scripts\Activate.ps1

python3 main.py
